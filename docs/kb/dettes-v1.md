# KB — Dettes datées avant la clôture de la V1

<!-- REGISTRE. Une dette se COMPTE et se DATE : on écrit ce qui manque, ce que
     ça coûte si on l'oublie, et le moment où elle doit être soldée. Une dette
     soldée se barre avec la date, elle ne s'efface pas. Créé le 21 septembre
     2026 (programme de nuit, point 5). -->

| # | Dette | Coût si oubliée | Échéance |
|---|---|---|---|
| 1 | **25 tests rouges à dates figées** (8 `booking-changes`, 3 `avis-endpoint`, 10 `messages-classify`, 4 `menages-public-filtre-presta`). Règle : dates relatives si le test lit l'horloge, dates figées s'il injecte `maintenant`. CLAUDE.md « DETTE DATÉE » porte le compte. | Le 26e rouge se noie dans « les 25 habituels ». C'est déjà arrivé une fois (lecture tronquée à 1000 lignes). | **Session dédiée avant la clôture V1 YieldFlow.** Pas à la volée dans un lot. |
| 2 | ~~Migration `2026-09-20-pilote-fenetre.sql`~~ **Soldée le 23 septembre 2026** : collée en prod par Thierry, contrôle conforme (toutes les fenêtres NULL). **Prouvée contre la prod le 23 septembre 2026** : `scripts/verifier-migrations-4-6.js` sur `.env.local` (projet cjmrizpdyhrcurmgyrhs, empreinte 5 biens ; staging en témoin : 3) — colonnes et table présentes. Vérifiée rétroactivement après la découverte de la dette 3. Par le SQL à empreinte collé en prod (`biens = 5`) : 3 CHECK de fenêtre, RLS activée et 1 policy sur `prix_hote` et `prix_hote_journal`. **Non prouvés** : la contrainte `prix_hote_nuit_unique` (dont dépend l'upsert de `lib/prix-hote.js`), les CHECK `evenement` / `geste`, les index — le script ne voit que les colonnes, la clé service passe la RLS (review V2.0.1). | — | — |
| 3 | ~~Migration `2026-09-21-fermetures.sql`~~ **Réellement soldée le 23 septembre 2026** — et non le 22 comme écrit d'abord. Le 23, l'API de production répondait « Could not find the table 'public.fermetures' » ; la migration collée par Thierry a rendu `lignes = 0` (table neuve, pas un cache périmé), puis `scripts/verifier-fermetures.js` sur `.env.local` a confirmé. Du 22 au 23, le 4.6.2 a tourné en prod SANS sa table : « table absente = aucune fermeture » (aucun bien piloté, aucune fermeture tentée, donc sans dégât). **Leçon** : une migration de prod se prouve par le vérificateur lancé CONTRE LA PROD, pas par le seul SQL de contrôle — qui répond aussi bien sur staging. | — | — |
| 4 | **« Pas encore ouverte » ne vient que de la fenêtre.** Le radar et `prix.html` disent « pas encore ouverte · s'ouvrira le … » par la seule règle de la fenêtre (4.6.0). Une nuit déjà ouverte au-delà de la fenêtre (fenêtre réduite après coup — le moteur 4.6.3 ne ferme jamais) est affichée « pas encore ouverte » alors qu'elle est en vente. | L'hôte lit « pas encore ouverte » sur une nuit vendable. | **4.6.4**, quand l'écran lira l'état réel de la nuit en plus de la fenêtre. |
| 5 | **`active_at` jamais remis à `null`** au retrait d'un bien : la quantité facturée ne décroît pas. | Un hôte qui retire un bien continue d'être compté pour lui. | Avant la fin de la beta (wiring Stripe). |
| 6 | **Purge du bien de test « colomier »** (jamais provisionné) : la suppression est bloquée par la cascade FK des tables enfant TEXT. | Un bien fantôme dans les listes de prod, un ménage ou un avis qui s'y rattache par erreur. | Chantier « identité du bien vs clé provider ». |
| 7 | **Vacances scolaires : la table s'arrête au 3 juillet 2027.** `lib/yield/vacances.js` vérifie la couverture et le DIT (jamais « hors vacances » par défaut). | À partir de l'été 2027, la référence yield tombe en « non calculable : zone non couverte » pour toutes les périodes de vacances. | **Importer 2027-2028 avant mars 2027** (publication ministérielle). |
| 8 | **`apps/menages/garde.html` : la bande de mois affiche encore les jours en redondance** sous chaque mois. Le correctif (jours seulement sous le mois, `renderMonthBand` en `<table class="month-band">` avec `<colgroup>` dans la table) n'est fait que dans `pages/biens-calendrier.html`, branche `ui-calendrier-jours`, en attente de verdict visuel. | Deux calendriers qui se veulent « même langage visuel » divergent. | Après le verdict sur `ui-calendrier-jours` : reporter la même bande, un seul commit. |
| 9 | **`apps/menages/public.html` : la case du calendrier ouvre la PREMIÈRE offre du jour** quand deux ménages sont libres le même jour (`cleOffre(libres[0])`). La liste, elle, distingue chaque offre par sa clé (bien, réservation) depuis 65a0ec4. | Une prestataire qui clique la case pour prendre le second ménage ouvre le premier. | Lot 3.4 (écran planning de garde), quand la case saura porter plusieurs offres. |
| 10 | **Le calendrier mobile ignore les fermetures** (4.6.2) : « Disponibilité → Fermé » écrit `avail = 0` + `stop_sell = true` sans objet. Sur un bien piloté, cette nuit est « fermée calculée » aux yeux du moteur. | Le 4.6.3 pourrait considérer rouvrable une nuit que l'hôte a fermée depuis son téléphone. Le canal, lui, ne rouvre jamais une nuit déjà fermée — filet tenu jusqu'à ce que le 4.6.3 tranche. | Refonte mobile (garde-fou produit : avant d'annoncer la messagerie). |
| 11 | ~~La raison d'une fermeture demandée par `window.prompt`~~ **Soldée le 21 septembre 2026** : la fermeture se crée par le parcours « nouvelle réservation » (type « indisponible ») et se modifie par sa fiche. | — | — |
| 13 | **`lib/fermetures.js` réimplémente des helpers existants** (`UUID_RE`, `estJour`, `decaler`, énumération de jours) déjà présents dans `lib/yield/capacite.js` / `reference.js`, avec une borne différente (`NUITS_MAX` 1000 vs `JOURS_MAX` 2000). | Un correctif d'arithmétique de dates (UTC, borne) fait à un endroit n'atteint pas les autres. | Quand un helper de dates commun sortira de `lib/yield/` (le cœur ne doit pas dépendre du yield). |
| 12 | **`fermer` avec refus du writer** : l'objet est retiré, mais ce chemin n'a pas de test — aucun refus du writer n'est déclenchable sans tarif, et `fermer` n'en porte pas. | Faible : le seul refus possible est un échec d'écriture, déjà rendu en 4xx/5xx. | Quand le writer aura un refus sans tarif à tester. |

| 14 | ~~Le prix d'ouverture est le prix de base~~ **Soldée le 22 septembre 2026** : le pilote quotidien ouvre au prix de la règle (4.6.4) ; la mémoire puis le prix de base ne sont plus que des replis. | — | — |

| 15 | **Fenêtre de course entre le moteur et une indisponibilité posée au même instant** : le moteur lit `fermetures` puis le writer upserte quelques secondes plus tard ; une indisponibilité créée entre les deux serait écrasée (`stop_sell = false`) sur ses nuits. Une fois par jour et par bien, quelques secondes. | Une nuit fermée par l'hôte vendue. | Au writer (vérifier `fermetures` juste avant l'upsert pour l'origine `engine`), avec le 4.6.4 qui rendra le moteur quotidien plus lourd. |
| 16 | **Le moteur relit ce que le canal et le writer relisent** (`calendar_inventory` trois fois, `fermetures` deux fois par bien). | Aller-retours redondants dans un cron à 60 s. | Quand le canal acceptera un instantané fourni par l'appelant. |

| 17 | ~~Deux assemblées de la matière du prix~~ **Corrigée au lot V2.0.1 (23 septembre 2026), soldée au merge** : `api/yield-prix.js` passe par `preparerContexte` et `prixDeLaNuit` ; il n'importe plus la règle. Mesure AVANT (`scripts/verifier-parite-prix.js`, prod) : 0 divergence sur 155 nuits — la dette n'avait pas encore mordu. Preuve APRÈS : réponse de l'écran identique à l'ancienne sur 26 mois-logements (La bulle, Cœur de vie 23), parité 0 divergence (prod 155 nuits, staging 360). Un « écart latent de fenêtre de contexte » soupçonné en cours de route s'est révélé faux : la segmentation ne dépend pas de la borne haute du contexte (tests/parite-ecran-moteur.test.js). | — | — |

| 18 | ~~Migration `2026-09-22-prix-hote.sql`~~ **Soldée le 23 septembre 2026** : staging et prod, contrôles conformes. **Prouvée contre la prod le 23 septembre 2026** : `scripts/verifier-migrations-4-6.js` sur `.env.local` (projet cjmrizpdyhrcurmgyrhs, empreinte 5 biens ; staging en témoin : 3) — colonnes et table présentes. Vérifiée rétroactivement après la découverte de la dette 3. Par le SQL à empreinte collé en prod (`biens = 5`) : 3 CHECK de fenêtre, RLS activée et 1 policy sur `prix_hote` et `prix_hote_journal`. **Non prouvés** : la contrainte `prix_hote_nuit_unique` (dont dépend l'upsert de `lib/prix-hote.js`), les CHECK `evenement` / `geste`, les index — le script ne voit que les colonnes, la clé service passe la RLS (review V2.0.1). | — | — |

| 19 | **Un mois fermé à la vente affiche « -100 % » contre son N-1** : le drapeau `periode_fermee_a_la_vente` de `lib/yield/pickup.js` (« zéro vendu n'est pas un échec ») n'est pas dans `DISQUALIFIENT_LE_N1`, et `drapeaux` n'est lu nulle part dans `apps/yield/prix.html`. Déjà vrai au pied de mois ; le CA dans le bandeau (f6a6050) puis sur les douze tuiles du radar (e996a02) le rend plus visible. `prM.drapeaux` porte déjà `periode_fermee_a_la_vente`, il suffit de le lire. | L'hôte lit une chute là où il a fermé lui-même. | Avec le 4.6.5 (alarmes) : lire `drapeaux` à l'écran et remplacer le pourcentage par « fermé à la vente ». |

| 20 | **Le calendrier ne connaît pas « pas encore ouverte »** (recette 4.6.3, pièce 2, 22 septembre 2026). `pages/biens-calendrier.html` lit une nuit sans ligne comme fermée (`jourFerme`, l. 731) : sur un bien piloté, toute nuit au-delà de la fenêtre (≈ 245 sur le Loft de recette) s'y affiche comme une nuit fermée à la main sans objet. Seul `apps/yield/prix.html` fait la distinction voulue par l'arbitrage C. La vendabilité est juste, c'est le mot qui ment. Choix de Thierry : noter, ne pas bloquer le merge. | L'hôte cherche une décision qu'il n'a jamais prise, et ne distingue plus une nuit fermée à la main d'une nuit que la fenêtre n'a pas encore atteinte. | Avec la dette 4 (l'écran lit l'état réel en plus de la fenêtre) : même règle, appliquée au calendrier. |
| 21 | **Une nuit rouverte à la main peut rester sans prix** (constaté en recette 4.6.3, pièce 2) : les 10-16 mars 2027 du Loft de recette sont `stop_sell = false`, `avail = 1`, `rate = null` depuis la recette du 4.6.2 (21 septembre, 23 h 08). `api/yield-prix.js` y affiche le prix de base (règle revue le 13 septembre), or une date sans prix part fermée vers les plateformes (8 septembre 2026). **Cause (lecture du code, même soir)** : une fermeture posée sur des nuits SANS ligne (au-delà de la fenêtre) crée des lignes `stop_sell = true` sans `rate` ; les rouvrir — `rouvrir_fermeture`, `modifier_fermeture` (nuits retirées) ou « Rouvrir à la vente » (`save`) — passe par `lib/calendrier-writer.js` (l. 351-379), qui ouvre sans vérifier qu'un prix existe et sans le dire (il avertit l'inverse, « tarifée mais fermée », pas « ouverte sans prix »). Bien piloté : le moteur tarife la nuit quand la fenêtre l'atteint (vu sur les 13-16 mars, 22 septembre). Bien non piloté : jamais. | Sur un bien relié, l'hôte lit 95 € « en vente » sur une nuit que les plateformes ne vendent pas. | Lot suivant, à la porte de l'hôte (refuser ou avertir l'ouverture d'une nuit jamais tarifée) ; vérifier sur un bien relié ce que Channex fait d'une date ouverte sans prix. |
| 22 | **Désactiver le pilote efface la main de l'hôte** (recette 4.6.3, pièce 4, 22 septembre 2026). `api/yield-pilote.js:200` appelle `viderPrixHote` au retour en calendrier : à la réactivation, le moteur écrase des prix que l'hôte croit encore les siens, sans que rien ne l'ait dit. Vécu en recette : 150 € (5 oct.) et 120 € (6 nov.) démarqués par un aller-retour du bouton, cause établie par déduction seulement — `prix_hote` n'a pas d'historique, `calendar_inventory` non plus, le journal des prix est vide sur un bien non relié. **Décision de Thierry : (a)** — les marques survivent à la désactivation ; à la réactivation, chacune reprend le prix affiché au calendrier, et la confirmation dit « N nuits gardent votre prix ». Plus une trace de la vie d'une marque (posée, retirée, par quel geste, quand). | La règle « la main de l'hôte prime » tient sauf si l'hôte touche au bouton : il perd ses prix sans le savoir. | **Corrigée le 23 septembre 2026** (`lib/prix-hote.js` : `recalerPrixHote`, `annulerPose`, `prix_hote_journal` ; `api/yield-pilote.js` ne vide plus ; tests `tests/prix-hote-survie.test.js`). Se solde après la review et le test court de Thierry sur staging. |
| 23 | ~~Migration `2026-09-23-prix-hote-journal.sql`~~ **Soldée le 23 septembre 2026** : collée sur staging puis en prod, contrôles conformes (prod : 0 ligne, 2 index, 1 policy, RLS active). **Prouvée contre la prod le 23 septembre 2026** : `scripts/verifier-migrations-4-6.js` sur `.env.local` (projet cjmrizpdyhrcurmgyrhs, empreinte 5 biens ; staging en témoin : 3) — colonnes et table présentes. Vérifiée rétroactivement après la découverte de la dette 3. Le contrôle SQL collé ce jour-là rendait déjà 0 ligne quand staging en portait 1 : il venait bien de la prod. Par le SQL à empreinte collé en prod (`biens = 5`) : 3 CHECK de fenêtre, RLS activée et 1 policy sur `prix_hote` et `prix_hote_journal`. **Non prouvés** : la contrainte `prix_hote_nuit_unique` (dont dépend l'upsert de `lib/prix-hote.js`), les CHECK `evenement` / `geste`, les index — le script ne voit que les colonnes, la clé service passe la RLS (review V2.0.1). | — | — |
| 24 | **Agrandir la fenêtre d'un bien déjà piloté ne prévient pas** (changement de dessin du 23 septembre 2026) : l'activation annonce « N nuits ont déjà un prix au calendrier ; YieldFlow va les remplacer », mais « Modifier » la fenêtre (`enregistrerFenetre`, `apps/yield/prix.html`) écrit sans confirmation, et les nuits qui entrent dans la fenêtre sont recalculées au passage suivant. | Un hôte qui a préparé des prix au-delà de sa fenêtre les voit remplacés sans l'avoir lu. | Avec le lot suivant : la même confirmation (compte par `?fenetre_type=&fenetre_valeur=`, déjà servi par l'endpoint) sur le changement de fenêtre. |
| 25 | ~~Capacité non calculable : le pilote pose quand même les prix, puis dit « aucun prix posé »~~ **Corrigée au lot V2.0.3 (23 septembre 2026), soldée au merge** : `piloterLeBien` ne tarife plus rien quand la capacité relue n'est pas calculable, et le bilan le dit (`ouverture_inconnue`) ; le docstring de `calculerPrix` dit désormais ce que fait le code (`ouverts: null` = pas de filtre, c'est l'appelant qui refuse). Test : `tests/pilote-quotidien.test.js`, « capacite non calculable — AUCUN prix demande » — rouge sur l'ancien code, vert sur le nouveau. | — | — |
| 26 | **La référence du cœur est le prix voyageur TOTAL, ménage compris — la grille V1 est gonflée pour un bien qui facture un ménage** (ouverte le 23 septembre 2026, chantier nouveau bien, règles 12 et 18). La grille (`construireGrille`, via `eclater` → `prixVoyageur`) est bâtie sur le prix voyageur total ; le moteur pousse un TARIF NUITÉE. Pour un bien avec ménage, chaque niveau porte le ménage amorti sur la durée des séjours passés. **Exposition réelle aujourd'hui : Colomiers**, ménage de 15 à 20 € sur ses 12 réservations Airbnb depuis juillet 2026 (9 % de son CA Airbnb) — en mode calendrier, historique trop court pour une grille fiable, donc sans effet encore. Cœur de vie 23 : ménage facturé jusqu'en février 2024, encore dans la fenêtre de 3 ans (sept. 2023 → fév. 2024, +10 à +36 % selon le mois). **Coût d'une référence en tarif nuitée, mesuré sur 3 ans de réservations confirmées** : Airbnb isolable à 100 % (Beds24 : `rateDescription` « Base Price », 813/813 ; Channex : `notes` « Listing Base Price » ou `rooms[].days` + service « Cleaning Fee », 43/43) ; Channex Booking et Offline : `rooms[].days`, 17/17 ; **Beds24 Booking : 9/205 seulement** (ligne « frais de ménage » présente) et **Beds24 direct : 0/84** — pour les autres, rien ne dit si le ménage est absent ou fondu dans le prix. **Règle pour ces lignes ambiguës, ACCEPTÉE par Thierry (23 septembre 2026)** : hériter de ce que dit l'Airbnb DU MÊME BIEN sur la même période (pas de ménage chez Airbnb → prix = tarif nuitée) ; sinon les marquer « tarif nuitée non isolable » et les écarter de la GRILLE seulement, jamais du CA. **Exigence** : c'est une INFÉRENCE, pas une mesure — un hôte peut facturer le ménage sur un canal et pas sur l'autre. Chaque nuit porte un drapeau dans la donnée, **« tarif déduit » contre « tarif mesuré »**, et les deux ne se confondent jamais dans un même calcul sans que le drapeau suive (même règle que la capacité passée ESTIMÉE, docs/kb/indicateurs-yield.md). Travail : une fonction `tarifNuitee` à côté de `prixVoyageur` dans `lib/yield/eclatement.js`, un `tarif_par_nuit` dans l'éclatement, la grille et la référence qui le lisent, les indicateurs qui gardent le prix voyageur ; aucune migration ; après la dette 17 (une seule assemblée). Ordre de grandeur : un sous-lot. | Le premier hôte externe qui facture un ménage verra sa grille gonflée d'autant, et la bascule V2 marché → réel ferait un saut de prix. | **Avant le premier hôte externe qui facture un ménage, et avant la V2.6 (bascule)** — inscrit en V2.0. |

## Règle du registre

Une dette se **note** au moment où on décide de ne pas la solder, pas au moment
où on s'en souvient. Elle porte une **échéance liée à un lot** (« avant le
4.6.3 ») plutôt qu'une date : c'est la checklist de ce lot qui la rappellera.
